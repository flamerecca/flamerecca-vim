version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run: apt-get update && apt-get install -y sudo curl git vim ncurses-base ncurses-term
      - run: sudo chmod 777 ~/.terminfo
      - run: sudo su -c 'bash <(curl https://bit.ly/flamerecca-vim -L)'
